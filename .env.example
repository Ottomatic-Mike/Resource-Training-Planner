# Training Plan Manager - Environment Configuration
# Copy this file to .env and fill in the values for your environment.
#
# STANDALONE MODE (default):
#   Leave SSO_ENABLED commented out or set to false.
#   Users provide their own API keys in the browser.
#
# CORPORATE / SSO MODE:
#   Set SSO_ENABLED=true and configure your identity provider below.
#   API keys are managed server-side - users just log in.

# ==============================================================================
# Core Settings
# ==============================================================================
NODE_ENV=production
PORT=3000

# ==============================================================================
# SSO Authentication (set to true to require login)
# ==============================================================================
# SSO_ENABLED=true

# Session secret - CHANGE THIS to a random string in production
# Generate one with: node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
# SESSION_SECRET=change-this-to-a-long-random-string

# Set to true if behind a reverse proxy that terminates HTTPS (e.g., nginx, AWS ALB)
# TRUST_PROXY=true

# ==============================================================================
# SSO Protocol: Choose ONE of OIDC or SAML
# ==============================================================================
# SSO_PROTOCOL=oidc

# --- Option A: OpenID Connect (OIDC) ---
# Works with: Azure AD, Okta, Google Workspace, Auth0, Keycloak, OneLogin,
#             Ping Identity, AWS SSO, or any OIDC-compliant provider.
#
# Azure AD example:
#   OIDC_ISSUER=https://login.microsoftonline.com/{tenant-id}/v2.0
#
# Okta example:
#   OIDC_ISSUER=https://{your-org}.okta.com
#
# Google Workspace example:
#   OIDC_ISSUER=https://accounts.google.com
#
# Auth0 example:
#   OIDC_ISSUER=https://{your-domain}.auth0.com
#
# Keycloak example:
#   OIDC_ISSUER=https://{host}/realms/{realm}

# OIDC_ISSUER=
# OIDC_CLIENT_ID=
# OIDC_CLIENT_SECRET=
# OIDC_CALLBACK_URL=https://your-domain.com/auth/callback

# --- Option B: SAML 2.0 ---
# Works with any SAML 2.0 identity provider (ADFS, Shibboleth, etc.)
# Set SSO_PROTOCOL=saml to use this instead of OIDC.
#
# SAML_ENTRY_POINT=https://idp.example.com/sso/saml
# SAML_ISSUER=training-plan-manager
# SAML_CERT=MIIDpDCCAoygAwIBA...  (IdP signing certificate, base64, no line breaks)
# SAML_CALLBACK_URL=https://your-domain.com/auth/callback

# ==============================================================================
# Server-Side AI API Keys (used only when SSO_ENABLED=true)
# ==============================================================================
# When SSO is enabled, the server injects these keys into AI API requests.
# Users never see or need to manage API keys.
# Only configure the providers you want to offer.

# Anthropic (Claude) - https://console.anthropic.com/
# ANTHROPIC_API_KEY=sk-ant-api03-...

# OpenAI (GPT) - https://platform.openai.com/
# OPENAI_API_KEY=sk-...

# Google (Gemini) - https://aistudio.google.com/apikey
# GOOGLE_API_KEY=AIza...
